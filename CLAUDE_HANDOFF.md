# Claude Code 下一阶段执行交接

日期：2026-09-24。交接实现基线：`d726700de492e2c23efad69453815a338cbab819`。这是一份下一阶段任务书；实际完成状态以 PROJECT_STATUS.md 最新记录及代码为准。先读 AGENTS.md。

## 1. 用户最新纠正：必须扩大题型覆盖

用户指出：“只有这两种有什么用？我们有很多类型……这只是感知类型题里面一个计算。”

当前只有最大间距、等权回转半径两种模板接通了数值→四选一试跑。不要把这说成整个 benchmark 的通用生产能力，也不要把下一阶段主要精力继续花在这两个模板重复扩量。

本阶段目标：整理已有任务与可复用实现，建立可扩展的题型接口，在感知、推断、设计选择三个能力方向推进代表性模板，并把覆盖情况和实际运行展示到网站。先有可信模板，再批量换结构。不要重新发明导师分类；任务模板是实现层，必须映射回导师的能力、领域和双尺度。

下文具体实施顺序和少量试跑规模是工程建议，不是导师额外规定的硬配额。遇到真实证据缺口应记录并继续可独立推进的其他模板，不凑题、不因一项阻塞停掉全批。

## 2. 接手时先核对

1. 检查分支、未提交文件、远端差异；不重置、不强推。先读取 CLAUDE.md、AGENTS.md、PROJECT_STATUS.md 顶部，再读 PROTOTYPE.md、RUNBOOK.md、DATAFLOW.md、METHODOLOGY.md、ANSWER_FORMATS.md。
2. 核对 docs/data/catalog.json、focused-screening.json 及真实代码，勿把 PROJECT_STATUS.md 下半部历史“80道全部补满”误作当前状态。
3. 当前原始候选80道：42保留待审，32小修后复查，6重构；后38道不在 active 配额。原型目标160，当前缺118。无本轮新增人工批准。
4. 12个模板试跑为已有题的格式衍生，不增加题库数量，不等于完整重跑M0–M6。
5. 同机有 runs/template-pilot-v01、runs/choice-pilot-v01、runs/choice-pilot-v02。最新选择题展示对应v02。runs/与inputs/被Git忽略，换机器时不能假定存在，也不能从网页摘要伪造历史运行。

## 3. 已有实现与边界

| 位置 | 已完成 | 下一步复用方式 |
|---|---|---|
| pipeline.py、builder_modules/ | M0–M6材料、AI packet、接受记录、校验与导出 | 保留模块职责及冻结运行机制 |
| builder.py | 距离/夹角计算、schema与语义检查 | 盘点真实接口与单测，迁移可用计算器 |
| template_engine.py | 最大间距、等权Rg，数值与Decimal复核 | 可拆出统一模板接口；保留数值正确性检查 |
| choice_engine.py | 两类数值的错误机制干扰项、去重、容差、确定性排序、单字母评分 | 提取共用选项检查/导出；不同题型自有干扰项和证据规则 |
| templates/registry.json | 六类实现/计划状态；并非完整任务分类 | 扩展为有证据的覆盖清单，明确已实现/仅旧代码/计划/阻塞 |
| templates/answer-policy.json、ANSWER_FORMATS.md | 四项唯一正确；设计选择与开放生成区别 | 新模板统一遵守，不把规则文件当已执行验证 |
| docs/templates.html、templates.js、choices.js | 实例选择、数值复算、四选一试答、审核记录、代码 | 扩展为能查看更多题型的工作台 |
| tools/export_template_workbench.py、export_choice_workbench.py | 仅允许既有公开pilot材料，检查快照 | 新批次需显式公开材料清单与检查，不能直接放开私有run导出 |

注意：M3 `builder_modules/m3_tasks/module.py` 与 `builder.py` 都仍拒绝 eligible 的 design；现有可注册验证器范围有限。设计选择需要补明确契约、评价器和测试，再修改相关门槛；禁止简单删去判断让所有设计任务通过。

数值模板试跑仍为专用入口，尚未自动接入通用M4流程。若做统一入口，必须写清哪些步骤真实调用、哪些仍由助手按提示词完成；不能把多个独立脚本包装成已无人值守端到端系统。

## 4. 下一阶段按此顺序执行

### A. 先盘点，产出题型覆盖表

读取现有候选、筛查记录、计算脚本、模板注册表和相应测试。按“科学任务/答案判定方式”归并，不能每题一个模板，也不能所有数值题只算一种能力。

建议维护一份机器可读主表及其网页展示，不制造多个手工重复事实来源。每行至少包含：

- family_id、名称、所属能力、考点与学科意义；
- 可适用领域、输入尺度与实际推理尺度的定义及限制；
- 输入材料、必要物理条件、输出形式；
- 来源依据、已有示例题号、已有代码/测试路径；
- 正确答案取得方式、三个干扰项来源、四项验证方法；
- 实现状态、缺材料/计算器/证据/审核的具体原因、实施顺序。

旧31个规划方向不是导师规定的31种题型，不必恢复或填满。当前registry的6项也不是覆盖全集。先对实际有据可查的任务形成主表，新增规划项标明“待依据”，不得为好看捏造文献支持。

### B. 做可扩展接口，并跑通不同能力

共用：输入/来源检查、模板版本、构题输出、四项契约、映射、记录、导出、评分。各模板负责：考点、必要输入、正确答案计算或证据、错误项及验证。纯确定性部分用代码；科学证据提取/任务选择可用固定提示词和结构化输出，不默认要求额外API。

优先查证这些已有材料可能支持的路线，下面是候选路线而非预先认定通过的题型：

| 能力 | 优先考察 | 验证边界 |
|---|---|---|
| 感知 | 已有距离/夹角、接触或配位、构象比较 | 阈值/选点/结构身份有依据；接触不等于化学键，数值不自动证明机制 |
| 推断 | 已有力与位移、几何与物理关系、衍射等材料 | 明确模型/假设/适用条件；去除相关几何后不应仍能拿主要分数；避免给答案再做减法 |
| 设计选择 | 论文优化案例或有性质证据的候选结构集 | 给初始结构、目标、约束、四个实际候选；逐项可判定，唯一正确，未知不能当错误 |

先选择材料和验证最充分的模板实施，每个新模板用少量不同真实输入（建议2–3个，视材料而定）验证复用。至少分别推进感知迁移与推断模板；设计选择同步检索四结构/性质证据，能取得则实做，缺证据则交付明确缺口与已尝试来源。不要停在一张计划表，也不要把感知题换标签冒充推断/设计。

新题依RUNBOOK保留M0–M6实际证据与记录；适配器试跑应明确标注其覆盖模块，不冒称重跑完整流程。改变旧题或规则使用新run，不覆盖旧答案/审核快照。

### C. 同时处理已有四选一的质量风险

12例数值/唯一性全部通过，但若最大间距题选最大值、Rg题选最小值，可答对7/12。这个结果是对已保存选项的规则统计，不是模型测试。

保留这项诊断，并增加适用的新模板诊断。改进干扰项要有错误机制，不能围绕正确值随便加减凑数；不能只换随机种子、美化字母分布来掩盖大小规律。必要时增加新的真实结构再检查，避免只针对12例调参。程序通过与难度通过分别报告。

此项与扩展题型并行推进，不要再次变成只优化两个模板的整轮工作。未知科学语义仍待人工审核。

### D. 网站让用户看懂覆盖与实现

保留导师坐标系及现有统一完整QA展示。在题型工作台展示：题型→能力→输入→构题/答案方法→验证→实际例子→代码/提示词→缺口。区分“支持多少题型”和“生成多少实例”。

每个已运行实例能查看完整学生输入、A/B/C/D、审核侧答案/证据/错误机制、各阶段输入输出及失败记录。学生包不含答案键/解释；公开开发示例与未来封闭测试集分开。不得用空卡片计完成。

## 5. 完成标准与本轮回报

- 有据可查的题型覆盖主表已保存，网站可见；列出新增题型及未实现项。
- 至少把有材料支持的新任务模板实际跑通，报告感知、推断、设计选择各自完成或阻塞，不只提交提示词文字。
- 每种实做模板有版本、计算器或证据验证路径、真实试跑及错误/歧义测试；复用公共机制，不复制一套Builder。
- 四选一恰好四项唯一正确，保存三个错误依据；设计选择不是开放生成。
- 区分数值通过、语义/科学待审、难度待测、目录准入；所有未经人工批准者保持待审核。
- 修改代码运行适当测试；最近完整基线162项通过，后续大小排名诊断相关6项重跑通过。网页验证实际交互、附件/学生包、手机布局；不可虚报旧测试为本轮测试。
- 更新PROJECT_STATUS.md顶部，给出文件、run、题号、测试、阻塞、提交与Pages状态；旧记录保留为历史。
- 仅将允许公开的产物提交推送；不强推，不上传密钥、未许可全文、完整私有run。用户已授权现有仓库的正常同步，若本机凭据不可用如实报告，不借用未知账户。

## 6. 启动方式与复现提示

在仓库根目录工作。本轮先盘点实际状态并开始A/B，不要求用户手工阅读几十篇文章或逐题转贴提示词。读论文/数据库时核验原始来源；不要用摘要代替答案证据。

如确需在新机器重建已公开数值试跑，可用以下命令（它们只是既有功能的复现，不算本阶段新成果）：

```text
python template_engine.py --manifest templates/pilot-manifest.json --out runs/numeric-handoff-new
python choice_engine.py --source runs/numeric-handoff-new --out runs/choice-handoff-new --seed geobench-choice-v1
python -m unittest discover -s tests -v
```

已有目录不可覆盖；不必为接手重复运行已确认完整的旧批次。当前快照用于审计，独立复现需要匹配版本仓库及docs/assets，不是假定快照目录能独立安装。

项目：https://github.com/K0NG1212/geometry-qa-builder
网站：https://k0ng1212.github.io/geometry-qa-builder/
工作台：https://k0ng1212.github.io/geometry-qa-builder/templates.html

用户期待的最终回报：这次新增了哪些“可复用题型”、每种能做什么、实际出了哪些题、验证到哪一步、在哪个页面查看，以及下一步剩下什么。
