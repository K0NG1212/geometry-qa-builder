# 从填充目标到问题池：数据如何流动

## 先区分现有程序与完整工作流程

现有 v0.3 程序从已取得的 bundle 开始。之前的演示使用本地既有材料，M1 没有自动搜索论文。
下面补齐目标驱动的操作规程：目标解释与检索由 Codex 会话完成，现有程序接手格式化材料、
阶段校验、数值计算和导出。不能称为已经完成“输入一个格子即可无人值守填满”的应用。

“化学 × 1–10 nm”是覆盖目标，不是 Builder 的一个模块。必须区分输入对象尺度与推理尺度。
示例暂以输入尺度为目标；这不是用户已经启动的真实批次。实际尺寸未验证的题不能算作填充成功。

| 步骤 | 读取什么 | 具体工作 | 给下一步什么 | 当前执行方式 |
|---|---|---|---|---|
| M0 目标与配置 | 用户的填充需求 | 明确领域、尺度轴、长度范围、题量、来源路线 | goal.json + 运行配置 | Codex 整理目标；程序已有参数检查/快照，尚无目标自动路由 |
| M1 检索与材料整理 | goal.json | 检索原始论文/数据库/benchmark，取得全文和结构，记录来源，再标准化 | sources.json + 原始材料 + bundle.json + asset-units.json | 检索由 Codex 按 retrieval.md 执行；标准化由程序执行 |
| M2 科学证据 | 材料包 | 提取几何关系、科学结论、适用条件和原文依据 | evidence.json | AI + 格式/引用检查 |
| M3 任务规划 | 证据 + 原始输入 | 判断能测什么，指定能力、双尺度、输入与评分方法，排除不充分任务 | tasks.json | AI + 关联检查；不是只给题目贴标签 |
| M4 构题与答案 | 可执行任务 + 结构/证据 | 写具体问题、选择原子/结构、计算或构造答案 | construction-receipt.json + qa.json | AI 计划 + 距离/夹角程序；其他答案待审核 |
| M5 质量检查 | QA + 所有来源 | 检查能否作答、引用、条件、数值、泄漏与评分 | review.json + report.json | AI + 基础程序；科学专家审核尚未实现 |
| M6 整理入池 | 通过/待审/拒绝记录 | 导出统一格式，保留来源和状态，区分候选与正式题 | model-inputs.json + private-answers.json + quality-report.json | 已有本批导出；跨批合并和正式入池门槛尚未自动实现 |

每一步发现缺材料就回到 M1；发现证据不支持结论就回到 M2；题型无可执行评分回到 M3。
修改已接受阶段需 fork 或新建 run，不覆盖原记录。目标格子只用于规划和筛选，不代替证据。

## 怎么看实际输入输出

M0 的 goal.json 与 M1 的 sources.json 先由助手保存在本批的工作目录，目前不是程序自动产物。
每个已经初始化的 run 内保存 config.json、bundle.json、各阶段 packet、接受结果和失败记录。
packet 是该阶段真正提交给 AI 的上下文；evidence/tasks/qa/review.json 的 data 字段是接受的输出。

```text
python tools/inspect_run.py --run runs/你的批次 --html
```

生成该 run 的 run-view.html，可展开各模块已有输入和输出；未生成文件显示“未生成”。
它是本地记录查看器，不是质量认证。内含私有证据和答案，不应上传公开网站。
也可以直接对 Codex 说：“打开这轮 M2 的实际输入和输出，解释哪些证据通过、哪些被排除。”
公开模块工作台只展示流程说明和带标记的示意数据，不自动连接个人电脑中的 run。

## 现有 benchmark 怎么复用

仍需要来源、分类与质量检查，但不必重新生成已经存在的题目。

1. **完整 QA 可直接复用**：M0 确定目标 → M1 取得题目、结构、答案及许可 → M2 审核已有答案依据
   （复用已有证据，不必重新从论文提取）→ M3 映射我们的领域/双尺度/能力 → M4 只做格式适配
   → M5 检查 → M6 候选入池。保留原题 ID、版本、split、原文和改动记录，来源标记 reused。
2. **只有结构或性质数据**：它只是构题材料，M1 后仍走 M2–M6，需要设计新题。
3. **原题需要改写或补结构**：在 M4 修改，标记 adapted，并重新检查答案；不是原题原样复用。

上述复用分支是操作设计。目前没有通用 benchmark 导入适配器或跳过生成阶段的 CLI；
不能直接把任意 benchmark JSON 塞进 accept。下一步按选定数据集编写字段映射和相应测试。
未知许可先保留核实，已有 benchmark 身份不自动证明适合再发布或科学质量达标。
公开旧题应与新构建题分组报告，避免把复用数量当成新增贡献，也便于分析既有数据暴露的影响。
