# 答案与选项规则

用户确认：后续选择题统一为 **四选一**。恰好四个选项 A/B/C/D，且在题目给定条件下恰好一个正确。模型只输出一个字母；解释、正确值、原文证据及其他三项为什么错保留在审核材料中。

当前状态（2026-09-24）：`task_families/` 已实现感知（命名键角、主链二面角、最大间距/回转半径 v0.2 选项）、推断（受力路径导数、衍射消光）与设计选择（构象候选选择，计算性质证据）的四选一族函数，运行 family-pilot-v01 共 29 例通过程序核验、1 例按规则记录失败；全部待人工审核，未计入题库。论文案例式设计选择与通用 M4 自动接入尚未实现；v0.1 运行保持不变。

| 能力 | 问什么 | 四个选项可以是什么 | 如何确定正确与错误 |
|---|---|---|---|
| 感知 | 距离、角度、回转半径、空间关系 | 四个数值、四个空间关系或四幅结构图 | 程序从实际输入计算；另外三项来自可解释的常见错误，不能也落入正确容差 |
| 推断 | 在给定几何与物理条件下，性质或行为如何 | 四个性质值、不重叠区间、趋势或物理结论 | 物理模型或论文证据支持正确项，其他项在相同条件下可被排除；未知不能冒充错误 |
| 设计选择 | 为达到明确目标，应选哪个结构或修改方案 | **四个候选结构**，或四种明确修改方案 | 检查目标、约束、结构有效性及候选性质证据；唯一一项满足条件或在预先规定评价中唯一最优 |

## 设计题的具体形式

题干：给定初始结构 S0、目标 P、允许修改的范围和固定条件，在 A/B/C/D 四个候选结构中选出满足要求的一个。

- 学生侧：S0、必要条件、四个结构的真实表示及一致的标签。按实际任务提供 XYZ/SDF、可读结构图或其它足够的表示；不能仅给四个无法理解的名称。
- 审核侧：每个候选的来源、身份与合法性、约束检查、目标性质依据、正确字母及另外三项不合格的理由。
- 真实论文的优化案例可作为来源，但“论文选择了 B”不能单独证明 A/C/D 不行。数据库中 S1/S2 不同也不能证明改善。
- 若两项都满足条件，修改题目/候选或增加有依据的区分条件；不得强行指定唯一答案。若其它三项只有未知性质，也不能标为已证实错误。
- 此任务测量候选设计的选择与评价；不得等同于已经测得开放式新结构生成能力。开放生成保留独立路线。

## 共用检查

1. 四项同类型、相同精度和单位、相近表达长度，不用排版或措辞泄漏答案。
2. 先确定正确结果，再生成三个有错误机制的干扰项；记录错误机制，不能随意凑三个数。
3. 数值选项按显示精度去重，检查容差区间；范围题必须定义边界，不能重叠或留下答案歧义。
4. 四项都要核验，不能只验证正确项。结构选项检查等价表示、对称/构象等价及重复；不得把同一个正确结构的旋转副本当错误项。
5. 使用记录了随机种子的选项排序，同步更新标签、答案、附件映射；批次检查正确位置分布，不总把正确项放 A。
6. 核心评分仅检查输出标签是否等于答案键。宽容的空白处理可以明确规定；不得从长解释中猜一个选项。标签比对不能替代科学正确性验证。

数值直接回答仍是可选形式，必须明示舍入、单位与容差。已有试跑不自动改写；新格式须新建记录，并记录与原题的对应关系。
