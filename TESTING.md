# v0.3 模块拆分验证 — 2026-09-14

- 48 项 Python 测试通过。七个模块各有 test_mN_*.py，可单独执行。
- 完整 v0.3 合成回放通过；旧 v0.2 run 的冻结程序仍可读取旧结果。
- 模块快照被修改、当前模块与 run 版本不一致时停止，避免静默换实现。
- 网站检查通过：七个模块切换、直达链接、真实代码/提示词展开、首页入口、390px 手机布局。
- 页面源码清单从公开模块目录导出，不包含真实论文或运行答案。

以上仍是工程验证，合成回放没有调用 AI。未测真实材料上的科学质量。

---

# v0.2 验证记录

日期：2026-09-14。使用本地 Python 标准库执行；未调用付费 API。

```text
python -m unittest discover -s tests -v
python tools/replay_demo.py --run runs/demo-v02-final
```

结果：37 项测试通过（17 项原有测试、20 项模块化测试）。
合成回放完成 evidence → tasks → construction → review → export，生成 1 条测试候选，
程序计算两点距离为 2 angstrom；benchmark_ready=false。

| 验证范围 | 结果 |
|---|---|
| 伪造引文、缺结构、错误编号/单位/容差 | 拒绝或筛查标记 |
| AI 填入数值答案、未确认坐标单位 | 构题导入拒绝 |
| 过期请求、篡改提示词/阶段/计划、缺失前序阶段 | 拒绝继续 |
| 失败记录、坏 JSON 原文保留、修正后继续 | 通过 |
| 已有证据复用、旧程序快照执行 | 通过 |
| 分支重跑保留原 run、清理下游结果、防止嵌套复制 | 通过 |
| 同一运行并发导入 | 第二个导入被锁拦住 |
| 题目输入/私有答案分离、XYZ 注释清理 | 通过；语义泄漏仍需审查 |
| 空输出、审核失败 | 不会标记 benchmark-ready |

这只是工程验证：示例阶段输出是手写 fixture，不是现场 AI 生成。
没有验证真实文献提取准确率、专家一致率、难度、跨来源重复或真实模型表现。
生成与检查数值共用计算器；软件测试有已知距离/角度参照，但不构成独立科学验证。
下一轮应固定少量真实材料，逐模块审核中间结果并记录错误类型，再决定优化哪一模块。
