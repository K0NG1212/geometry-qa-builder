Using the attached full MOF-5 structure (424 atoms) and the fractional coordinates you obtain by dividing each Cartesian coordinate by a=25.8247 angstrom, compute the structure factor F(hkl)=sum_j Z_j*exp(2*pi*i*(h*x_j+k*y_j+l*z_j)) for (h k l) = (1,0,0), (1,1,0), (2,0,0) and (1,1,1), using atomic number as the weight Z_j (Zn=30, O=8, C=6, H=1). Report the amplitude |F(hkl)| for each of the four reflections to one decimal place. Which reflections are systematically absent, and what single rule (in terms of the parities of h, k and l) predicts this pattern for a face-centered (F-centered) lattice?

The attachment is the complete full-framework symmetry-expanded structure, 424 atoms, Cartesian coordinates in angstrom. Coordinates are in angstrom (1 nm = 10 angstrom). Atom rows are numbered starting at 1, immediately after the two header lines. This is one symmetry-expanded conventional cubic cell, not a molecule; do not treat it as an isolated finite cluster or apply further periodic images beyond what is stated. Convert to fractional coordinates by dividing each x,y,z by a=25.8247.