In the attached vancomycin chain-B structure (PDB 1AA5, 132 atoms), row 3 is the CA atom of residue 1 (MLU) and row 122 is the CA atom of residue 7 (3FG). Compute the distance between these two rows, and compare it to the whole-molecule maximum pairwise distance already established for chain B (Dmax = 19.2646 angstrom, from the preceding question). Report the terminal distance in angstrom to four decimal places.

The attachment is the same complete chain-B structure used previously (S0003, 132 atoms). Coordinates are in angstrom (1 nm = 10 angstrom). Atom rows are numbered starting at 1, immediately after the two header lines. Row order matches the order in this attachment exactly; do not renumber or reorder atoms.