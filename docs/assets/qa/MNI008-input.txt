Using the attached full ZIF-8 structure (204 atoms) and the fractional coordinates you obtain by dividing each Cartesian coordinate by a=16.8303 angstrom, compute the structure factor F(hkl)=sum_j Z_j*exp(2*pi*i*(h*x_j+k*y_j+l*z_j)) for (h k l) = (1,0,0), (1,1,0), (2,0,0) and (1,1,1), using atomic number as the weight Z_j (Zn=30, N=7, C=6, H=1). Report the amplitude |F(hkl)| for each of the four reflections to one decimal place. Which reflections are systematically absent (amplitude effectively zero), and what single rule (in terms of h+k+l) predicts this pattern for a body-centered (I-centered) lattice?

The attachment is the complete full-framework symmetry-expanded structure, 204 atoms, Cartesian coordinates in angstrom. Coordinates are in angstrom (1 nm = 10 angstrom). Atom rows are numbered starting at 1, immediately after the two header lines. This is one symmetry-expanded conventional cubic cell, not a molecule; do not treat it as an isolated finite cluster or apply further periodic images beyond what is stated. Convert to fractional coordinates by dividing each x,y,z by a=16.8303 (the cell is cubic, so the same divisor applies to all three coordinates).