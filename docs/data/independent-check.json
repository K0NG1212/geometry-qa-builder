{
 "kind": "independent_check",
 "checker_version": "0.1.0",
 "origin": {
  "source": "family-pilot-v01"
 },
 "checked": 29,
 "passed": 29,
 "failed": [],
 "by_family": {
  "named_bond_angle": {
   "pass": 3
  },
  "backbone_torsion": {
   "pass": 3
  },
  "force_path_derivative": {
   "pass": 3
  },
  "kinematic_extinction": {
   "pass": 4
  },
  "conformer_target_selection": {
   "pass": 4
  },
  "extent_choice_v2": {
   "pass": 12
  }
 },
 "checker_sha256": {
  "checkers/__init__.py": "b424176da0e26fafcb424be028ec0e1df44a2abe887e8c37002ebe9d6db464d6",
  "checkers/common.py": "b4306dfadd1849ecad4ebd62c99da2c5bda3261cc930c7e1ae266b705c095246",
  "checkers/families.py": "d02e0a28acd7e9492784630693cc25ce27d832b9f02f8d56cf8478fe2ce55475",
  "verify_all.py": "e7c220d15f95368a86b7d60c1e853f2e15f0978743ca36d6d8cbc2cf9931fda6"
 },
 "model_calls": 0,
 "scope": "Checks input→output, all four options, key agreement, answer-key leakage and source hashes. It does not judge scientific meaning or source interpretation; those remain sampled human/model review.",
 "results": [
  {
   "id": "FA-ANG-BCD",
   "family": "named_bond_angle",
   "checker_version": "0.1.0",
   "recomputed": "116.0251189438",
   "rounded": "116.03",
   "label": "D",
   "nearest_distractor_gap": "4.3251189438",
   "parameters": {
    "atoms": [
     [
      "C4",
      "4"
     ],
     [
      "O3",
      "45"
     ],
     [
      "C37",
      "37"
     ]
    ],
    "vertex": "O3"
   },
   "method": "law of cosines from three Decimal distances",
   "status": "pass"
  },
  {
   "id": "FA-ANG-WP6",
   "family": "named_bond_angle",
   "checker_version": "0.1.0",
   "recomputed": "119.9446786883",
   "rounded": "119.94",
   "label": "C",
   "nearest_distractor_gap": "2.3146786883",
   "parameters": {
    "atoms": [
     [
      "C3",
      "3"
     ],
     [
      "C1",
      "1"
     ],
     [
      "C4",
      "4"
     ]
    ],
    "vertex": "C1"
   },
   "method": "law of cosines from three Decimal distances",
   "status": "pass"
  },
  {
   "id": "FA-ANG-DMBCD",
   "family": "named_bond_angle",
   "checker_version": "0.1.0",
   "recomputed": "107.6182710356",
   "rounded": "107.62",
   "label": "B",
   "nearest_distractor_gap": "1.8517289644",
   "parameters": {
    "atoms": [
     [
      "C3",
      "3"
     ],
     [
      "O2",
      "44"
     ],
     [
      "CH3-C45",
      "45"
     ]
    ],
    "vertex": "O2"
   },
   "method": "law of cosines from three Decimal distances",
   "status": "pass"
  },
  {
   "id": "FA-TOR-1CRN-G20",
   "family": "backbone_torsion",
   "checker_version": "0.1.0",
   "recomputed": "106.2572069382",
   "rounded": "106.3",
   "label": "D",
   "nearest_distractor_gap": "98.9572069382",
   "parameters": {
    "atoms": "C(19)–N(20)–CA(20)–C(20)"
   },
   "method": "acos of plane normals with triple-product sign",
   "status": "pass"
  },
  {
   "id": "FA-TOR-1UBQ-V26",
   "family": "backbone_torsion",
   "checker_version": "0.1.0",
   "recomputed": "-46.4088436388",
   "rounded": "-46.4",
   "label": "B",
   "nearest_distractor_gap": "11.9911563612",
   "parameters": {
    "atoms": "N(26)–CA(26)–C(26)–N(27)"
   },
   "method": "acos of plane normals with triple-product sign",
   "status": "pass"
  },
  {
   "id": "FA-TOR-6LYZ-T43",
   "family": "backbone_torsion",
   "checker_version": "0.1.0",
   "recomputed": "-143.6458783156",
   "rounded": "-143.6",
   "label": "B",
   "nearest_distractor_gap": "12.9541216844",
   "parameters": {
    "atoms": "C(42)–N(43)–CA(43)–C(43)"
   },
   "method": "acos of plane normals with triple-product sign",
   "status": "pass"
  },
  {
   "id": "FA-FP-QM7X-7001",
   "family": "force_path_derivative",
   "checker_version": "0.1.0",
   "recomputed": "1.8479097251",
   "rounded": "1.8479",
   "label": "D",
   "nearest_distractor_gap": "0.9239097251",
   "parameters": {
    "direction": "A_to_B",
    "atoms": 14
   },
   "method": "Decimal atom-by-atom projection",
   "status": "pass"
  },
  {
   "id": "FA-FP-QM7X-7002",
   "family": "force_path_derivative",
   "checker_version": "0.1.0",
   "recomputed": "-0.4352795772",
   "rounded": "-0.4353",
   "label": "A",
   "nearest_distractor_gap": "0.0981795772",
   "parameters": {
    "direction": "B_to_A",
    "atoms": 11
   },
   "method": "Decimal atom-by-atom projection",
   "status": "pass"
  },
  {
   "id": "FA-FP-QM7X-7035",
   "family": "force_path_derivative",
   "checker_version": "0.1.0",
   "recomputed": "5.5319173445",
   "rounded": "5.5319",
   "label": "B",
   "nearest_distractor_gap": "0.5067826555",
   "parameters": {
    "direction": "A_to_B",
    "atoms": 17
   },
   "method": "Decimal atom-by-atom projection",
   "status": "pass"
  },
  {
   "id": "FA-EXT-NACL",
   "family": "kinematic_extinction",
   "checker_version": "0.1.0",
   "label": "C",
   "recomputed": "(3 1 0)",
   "parameters": {
    "weights": {
     "Cl": 17,
     "Na": 11
    },
    "a_A": 5.64056,
    "atoms": 8
   },
   "options": {
    "A": 1.0,
    "B": 0.21428571,
    "C": 0.0,
    "D": 1.0
   },
   "weakest_nonzero": 0.214286,
   "method": "separate cos/sin sums over the student table",
   "status": "pass"
  },
  {
   "id": "FA-EXT-DIAMOND",
   "family": "kinematic_extinction",
   "checker_version": "0.1.0",
   "label": "D",
   "recomputed": "(2 2 2)",
   "parameters": {
    "weights": {
     "C": 6
    },
    "a_A": 3.56679,
    "atoms": 8
   },
   "options": {
    "A": 0.70710678,
    "B": 0.70710678,
    "C": 1.0,
    "D": 0.0
   },
   "weakest_nonzero": 0.707107,
   "method": "separate cos/sin sums over the student table",
   "status": "pass"
  },
  {
   "id": "FA-EXT-SALEM2",
   "family": "kinematic_extinction",
   "checker_version": "0.1.0",
   "label": "A",
   "recomputed": "(1 1 1)",
   "parameters": {
    "weights": {
     "C": 6,
     "H": 1,
     "N": 7,
     "Zn": 30
    },
    "a_A": 16.8303,
    "atoms": 204
   },
   "options": {
    "A": 0.0,
    "B": 0.14600426,
    "C": 0.16411769,
    "D": 0.2762629
   },
   "weakest_nonzero": 0.146004,
   "method": "separate cos/sin sums over the student table",
   "status": "pass"
  },
  {
   "id": "FA-EXT-MOF5",
   "family": "kinematic_extinction",
   "checker_version": "0.1.0",
   "label": "B",
   "recomputed": "(1 1 0)",
   "parameters": {
    "weights": {
     "C": 6,
     "H": 1,
     "O": 8,
     "Zn": 30
    },
    "a_A": 25.8247,
    "atoms": 424
   },
   "options": {
    "A": 0.28045276,
    "B": 0.0,
    "C": 0.61651269,
    "D": 0.08730021
   },
   "weakest_nonzero": 0.0873,
   "method": "separate cos/sin sums over the student table",
   "status": "pass"
  },
  {
   "id": "FA-CONF-7050-max_dipole",
   "family": "conformer_target_selection",
   "checker_version": "0.1.0",
   "label": "D",
   "recomputed": "D",
   "parameters": {
    "goal": "max_dipole",
    "atoms": 16
   },
   "options": {
    "A": 0.57848,
    "B": 0.722212,
    "C": 0.638431,
    "D": 1.043318
   },
   "lead": 0.321106,
   "policy_margin": 0.1,
   "method": "own bond graph, handedness and distance-matrix checks; property matched by coordinates",
   "status": "pass"
  },
  {
   "id": "FA-CONF-7206-min_energy",
   "family": "conformer_target_selection",
   "checker_version": "0.1.0",
   "label": "A",
   "recomputed": "A",
   "parameters": {
    "goal": "min_energy",
    "atoms": 13
   },
   "options": {
    "A": -20175.79801,
    "B": -20175.647611,
    "C": -20175.736603,
    "D": -20175.650674
   },
   "lead": 0.061407,
   "policy_margin": 0.0434,
   "method": "own bond graph, handedness and distance-matrix checks; property matched by coordinates",
   "status": "pass"
  },
  {
   "id": "FA-CONF-7095-min_energy",
   "family": "conformer_target_selection",
   "checker_version": "0.1.0",
   "label": "B",
   "recomputed": "B",
   "parameters": {
    "goal": "min_energy",
    "atoms": 15
   },
   "options": {
    "A": -18085.093111,
    "B": -18085.278387,
    "C": -18085.032646,
    "D": -18085.069543
   },
   "lead": 0.185276,
   "policy_margin": 0.0434,
   "method": "own bond graph, handedness and distance-matrix checks; property matched by coordinates",
   "status": "pass"
  },
  {
   "id": "FA-CONF-7032-max_dipole",
   "family": "conformer_target_selection",
   "checker_version": "0.1.0",
   "label": "C",
   "recomputed": "C",
   "parameters": {
    "goal": "max_dipole",
    "atoms": 17
   },
   "options": {
    "A": 1.123048,
    "B": 1.115202,
    "C": 1.301605,
    "D": 1.119083
   },
   "lead": 0.178557,
   "policy_margin": 0.1,
   "method": "own bond graph, handedness and distance-matrix checks; property matched by coordinates",
   "status": "pass"
  },
  {
   "id": "FC-QNP007",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "10.0002886395",
   "rounded": "10.0003",
   "label": "C",
   "nearest_distractor_gap": "0.9800886395",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 41
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-QNP008",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "11.8783581855",
   "rounded": "11.8784",
   "label": "A",
   "nearest_distractor_gap": "0.6755418145",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 57
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-QNP009",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "14.0869752409",
   "rounded": "14.0870",
   "label": "B",
   "nearest_distractor_gap": "1.3401247591",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 64
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-QNP010",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "4.2466757170",
   "rounded": "4.2467",
   "label": "C",
   "nearest_distractor_gap": "0.2635757170",
   "parameters": {
    "measure": "equal_weight_rg",
    "atoms": 48
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-QNP011",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "4.6156086064",
   "rounded": "4.6156",
   "label": "D",
   "nearest_distractor_gap": "0.2279086064",
   "parameters": {
    "measure": "equal_weight_rg",
    "atoms": 70
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-MNP006",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "17.8512288891",
   "rounded": "17.8512",
   "label": "A",
   "nearest_distractor_gap": "0.9655711109",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 12
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-MNP007",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "23.9458638075",
   "rounded": "23.9459",
   "label": "B",
   "nearest_distractor_gap": "2.3128361925",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 32
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-MNP010",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "30.6102126568",
   "rounded": "30.6102",
   "label": "B",
   "nearest_distractor_gap": "3.3999873432",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 424
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-CNP007",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "19.5697811945",
   "rounded": "19.5698",
   "label": "C",
   "nearest_distractor_gap": "0.7934811945",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 132
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-CNP008",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "19.2645768965",
   "rounded": "19.2646",
   "label": "A",
   "nearest_distractor_gap": "1.5857231035",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 132
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-CNP009",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "28.7192483537",
   "rounded": "28.7192",
   "label": "B",
   "nearest_distractor_gap": "0.0857516463",
   "parameters": {
    "measure": "max_pair_distance",
    "atoms": 264
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  },
  {
   "id": "FC-CNP011",
   "family": "extent_choice_v2",
   "checker_version": "0.1.0",
   "recomputed": "8.2616830454",
   "rounded": "8.2617",
   "label": "C",
   "nearest_distractor_gap": "0.5069830454",
   "parameters": {
    "measure": "equal_weight_rg",
    "atoms": 264
   },
   "method": "Decimal (40 digits) explicit sums",
   "status": "pass"
  }
 ],
 "checker_code": {
  "checkers/__init__.py": "\"\"\"Independent answer checkers (see common.py for the independence rule).\"\"\"\nfrom .common import CheckError, VERSION, packet_shape\nfrom .families import CHECKERS\n\n\ndef check(packet, key):\n    \"\"\"Returns a report dict; status is 'pass' only if every check succeeded.\"\"\"\n    report = dict(id=packet['id'], family=packet['family'], checker_version=VERSION)\n    try:\n        if packet['id'] != key['id'] or packet['family'] != key['family']:\n            raise CheckError('Packet and key do not belong together')\n        if packet['family'] not in CHECKERS:\n            raise CheckError('No independent checker for family ' + packet['family'])\n        if not key.get('input_hashes'):\n            raise CheckError('No source asset recorded in the answer key')\n        packet_shape(packet)\n        report.update(CHECKERS[packet['family']](packet, key))\n        report['status'] = 'pass'\n    except CheckError as error:\n        report.update(status='fail', problem=str(error))\n    except (KeyError, ValueError, IndexError, ArithmeticError) as error:\n        report.update(status='fail', problem='Malformed packet or key: %s: %s' % (type(error).__name__, error))\n    return report\n",
  "checkers/common.py": "\"\"\"Shared helpers for independent answer checkers.\n\nIndependence rule: nothing under checkers/ imports the question-generation code\n(task_families, family_engine, template_engine, choice_engine or tools). A checker\nsees only what the tested model sees (question text, attached inputs, options) plus\nthe answer key, and recomputes with its own implementation. Provenance checks may\nread the public source asset named in the answer key and verify its SHA-256.\n\"\"\"\nimport hashlib\nimport math\nimport re\nfrom decimal import Decimal, ROUND_HALF_UP, localcontext\nfrom pathlib import Path\n\nROOT = Path(__file__).resolve().parents[1]\nVERSION = '0.1.0'\nLABELS = ['A', 'B', 'C', 'D']\n# Answer-key vocabulary that must never appear in a student packet.\nFORBIDDEN_KEYS = {'correct_label', 'is_correct', 'option_audit', 'reason', 'source_id', 'numeric_answer',\n                  'explanation', 'excluded_candidates', 'rank', 'checks'}\n\n\nclass CheckError(Exception):\n    \"\"\"A check that fails; the message is reported verbatim.\"\"\"\n\n\ndef need(condition, message):\n    if not condition:\n        raise CheckError(message)\n\n\ndef find(pattern, text, message, flags=0):\n    match = re.search(pattern, text, flags)\n    need(match is not None, message)\n    return match\n\n\ndef read_xyz(text):\n    \"\"\"Returns [(element, (Decimal x, y, z))]; strict count and row format.\"\"\"\n    lines = text.splitlines()\n    need(len(lines) >= 3 and lines[0].strip().isdigit(), 'XYZ header invalid')\n    rows = [line.split() for line in lines[2:] if line.strip()]\n    need(len(rows) == int(lines[0]), 'XYZ atom count does not match rows')\n    atoms = []\n    for row in rows:\n        need(len(row) == 4 and row[0].isalpha(), 'XYZ row must be element x y z')\n        coords = tuple(Decimal(v) for v in row[1:])\n        need(all(c.is_finite() for c in coords), 'Nonfinite coordinate')\n        atoms.append((row[0].capitalize(), coords))\n    return atoms\n\n\ndef dist(p, q):\n    with localcontext() as ctx:\n        ctx.prec = 40\n        return sum((a - b) ** 2 for a, b in zip(p, q)).sqrt()\n\n\ndef vec(p, q):\n    \"\"\"q - p as floats.\"\"\"\n    return [float(b) - float(a) for a, b in zip(p, q)]\n\n\ndef dot(u, v):\n    return math.fsum(a * b for a, b in zip(u, v))\n\n\ndef cross(u, v):\n    return [u[1] * v[2] - u[2] * v[1], u[2] * v[0] - u[0] * v[2], u[0] * v[1] - u[1] * v[0]]\n\n\ndef rounded(value, decimals):\n    return Decimal(str(value)).quantize(Decimal(1).scaleb(-decimals), rounding=ROUND_HALF_UP)\n\n\ndef sha256_file(rel):\n    path = (ROOT / 'docs' / rel).resolve()\n    need(path.is_relative_to((ROOT / 'docs/assets').resolve()), 'Evidence asset outside docs/assets')\n    need(path.exists(), 'Evidence asset missing: ' + rel)\n    return hashlib.sha256(path.read_bytes()).hexdigest(), path\n\n\ndef verify_hashes(key):\n    \"\"\"Every reviewer-side asset named in the key must match its recorded SHA-256.\"\"\"\n    paths = {}\n    for rel, digest in key['input_hashes'].items():\n        actual, path = sha256_file(rel)\n        need(actual == digest, 'Asset hash mismatch: ' + rel)\n        paths[rel] = path\n    return paths\n\n\ndef packet_shape(packet):\n    need([o['label'] for o in packet['options']] == LABELS, 'Options must be exactly A, B, C, D')\n    need(len({o['value'] for o in packet['options']}) == 4, 'Duplicate option values')\n    leaked = FORBIDDEN_KEYS & _keys(packet)\n    need(not leaked, 'Answer-key fields in student packet: ' + ', '.join(sorted(leaked)))\n\n\ndef _keys(obj):\n    if isinstance(obj, dict):\n        return set(obj) | set().union(*(_keys(v) for v in obj.values()))\n    if isinstance(obj, list):\n        return set().union(*(_keys(v) for v in obj)) if obj else set()\n    return set()\n\n\ndef gap(a, b, period=None):\n    d = abs(a - b)\n    if period is not None:\n        d = min(d % period, period - d % period)\n    return d\n\n\ndef judge_numeric(packet, key, value, period=None):\n    \"\"\"Recomputed value vs. options and key: exactly one option equals the rounded value,\n    the other three lie outside the tolerance, and key label / numeric key agree.\"\"\"\n    numeric = key['numeric_answer']\n    decimals, tol = numeric['decimals'], Decimal(numeric['tolerance'])\n    expected = rounded(value, decimals)\n    exact = Decimal(repr(value)) if isinstance(value, float) else Decimal(value)\n    units = {o.get('unit') for o in packet['options']}\n    need(units == {numeric['unit']}, 'Option units differ from the answer unit')\n    for o in packet['options']:\n        need(re.fullmatch(r'-?\\d+\\.\\d{%d}' % decimals, o['value']) is not None,\n             'Option %s not shown with %d decimals' % (o['label'], decimals))\n    hits = [o['label'] for o in packet['options'] if gap(Decimal(o['value']), exact, period) <= tol]\n    need(len(hits) == 1, 'Recomputed value matches %d options (need exactly 1)' % len(hits))\n    label = hits[0]\n    shown = next(o['value'] for o in packet['options'] if o['label'] == label)\n    need(Decimal(shown) == expected or (period and gap(Decimal(shown), expected, period) == 0),\n         'Matching option %s is not the rounded recomputed value %s' % (shown, expected))\n    need(Decimal(numeric['value']) == expected, 'Numeric key %s != recomputed %s' % (numeric['value'], expected))\n    need(key['correct_label'] == label, 'Key label %s != recomputed label %s' % (key['correct_label'], label))\n    nearest = min(gap(Decimal(o['value']), exact, period) for o in packet['options'] if o['label'] != label)\n    q = Decimal('1e-10')\n    return dict(recomputed=str(exact.quantize(q)), rounded=str(expected), label=label,\n                nearest_distractor_gap=str(nearest.quantize(q)))\n",
  "checkers/families.py": "\"\"\"One independent checker per task family. Each reads the student packet (what the\nmodel sees) and the answer key, re-derives the task parameters from the question text,\nrecomputes with a different formula than the generator, and verifies all four options.\nA parameter that cannot be read from the question text is a failure: the question is\nthen not answerable from its own text.\n\"\"\"\nimport json\nimport math\nimport re\nfrom decimal import Decimal, localcontext\nfrom .common import (CheckError, need, find, read_xyz, dist, vec, dot, cross, verify_hashes,\n                     judge_numeric, LABELS)\n\n# Covalent radii (Cordero et al. 2008) for the checker's own bonding rule.\nRADII = {'H': 0.31, 'C': 0.76, 'N': 0.71, 'O': 0.66, 'S': 1.05, 'Cl': 1.02, 'P': 1.07, 'F': 0.57}\n\n\ndef inputs_by_name(packet):\n    return {i['name']: i['text'] for i in packet['inputs']}\n\n\n# ---------------------------------------------------------------- perception\ndef named_bond_angle(packet, key):\n    \"\"\"Law of cosines on three interatomic distances (generator: vector dot product).\"\"\"\n    q = packet['question']\n    rows = re.findall(r'([\\w\\-]+) = row (\\d+)', q)\n    need(len(rows) == 3, 'Question must map exactly three named atoms to XYZ rows')\n    vertex = find(r'bond angle at ([\\w\\-]+)', q, 'Question does not name the angle vertex').group(1)\n    names = [n for n, _ in rows]\n    need(vertex == names[1], 'Named vertex is not the middle atom of the listed triple')\n    [text] = [i['text'] for i in packet['inputs'] if i['format'] == 'xyz']\n    atoms = read_xyz(text)\n    need(all(1 <= int(r) <= len(atoms) for _, r in rows), 'Row index outside the XYZ')\n    a, v, b = [atoms[int(r) - 1] for _, r in rows]\n    for end in (a, b):\n        limit = 1.25 * (RADII[end[0]] + RADII[v[0]])\n        need(float(dist(end[1], v[1])) <= limit, 'Named end atom is not bonded to the vertex')\n    va, vb, ab = dist(v[1], a[1]), dist(v[1], b[1]), dist(a[1], b[1])\n    with localcontext() as ctx:\n        ctx.prec = 40\n        cosine = (va * va + vb * vb - ab * ab) / (2 * va * vb)\n    angle = math.degrees(math.acos(max(-1.0, min(1.0, float(cosine)))))\n    verify_hashes(key)\n    return dict(judge_numeric(packet, key, angle), parameters=dict(atoms=rows, vertex=vertex),\n                method='law of cosines from three Decimal distances')\n\n\ndef _pdb_atoms(text):\n    atoms = {}\n    for line in text.splitlines():\n        if line.startswith('ATOM'):\n            need(line[16] == ' ', 'Alternate location in excerpt')\n            atoms[(line[12:16].strip(), int(line[22:26]))] = tuple(Decimal(line[c:c + 8]) for c in (30, 38, 46))\n    return atoms\n\n\ndef backbone_torsion(packet, key):\n    \"\"\"Normals' angle via acos, sign from (n1 x n2).b2 (generator: two atan2 forms).\"\"\"\n    q = packet['question']\n    spec = find(r'defined by atoms ((?:[A-Z0-9]+\\(\\d+\\)–){3}[A-Z0-9]+\\(\\d+\\))', q,\n                'Question does not define the four torsion atoms').group(1)\n    parts = [re.fullmatch(r'([A-Z0-9]+)\\((\\d+)\\)', p).groups() for p in spec.split('–')]\n    need('IUPAC' in q, 'Sign convention not stated')\n    [text] = [i['text'] for i in packet['inputs'] if i['format'] == 'pdb-excerpt']\n    atoms = _pdb_atoms(text)\n    pts = []\n    for name, residue in parts:\n        need((name, int(residue)) in atoms, 'Torsion atom %s(%s) missing from excerpt' % (name, residue))\n        pts.append(atoms[(name, int(residue))])\n    b1, b2, b3 = vec(pts[0], pts[1]), vec(pts[1], pts[2]), vec(pts[2], pts[3])\n    n1, n2 = cross(b1, b2), cross(b2, b3)\n    cosine = dot(n1, n2) / math.sqrt(dot(n1, n1) * dot(n2, n2))\n    angle = math.degrees(math.acos(max(-1.0, min(1.0, cosine))))\n    if dot(cross(n1, n2), b2) < 0:\n        angle = -angle\n    # Provenance: every excerpt line must occur verbatim in the public PDB file.\n    paths = verify_hashes(key)\n    if paths:     # check() requires a recorded source for every real instance\n        source = set(line.rstrip() for p in paths.values() for line in p.read_text(encoding='utf-8').splitlines())\n        need(all(line in source for line in text.splitlines() if line.startswith('ATOM')),\n             'Excerpt lines are not copied unchanged from the source PDB')\n    return dict(judge_numeric(packet, key, angle, period=Decimal(360)), parameters=dict(atoms=spec),\n                method='acos of plane normals with triple-product sign')\n\n\ndef extent(packet, key):\n    \"\"\"Decimal recomputation: Dmax by squared distances, Rg by centroid (explicit sums).\"\"\"\n    q = packet['question']\n    [text] = [i['text'] for i in packet['inputs'] if i['format'] == 'xyz']\n    atoms = read_xyz(text)\n    pts = [p for _, p in atoms]\n    need('mass weights' not in q or 'Do not use mass weights' in q, 'Weighting ambiguous')\n    with localcontext() as ctx:\n        ctx.prec = 40\n        if 'maximum Euclidean distance' in q:\n            best = max(sum((a - b) ** 2 for a, b in zip(p, r)) for i, p in enumerate(pts) for r in pts[i + 1:])\n            value, kind = best.sqrt(), 'max_pair_distance'\n        elif 'radius of gyration' in q:\n            n = len(pts)\n            c = [sum(p[k] for p in pts) / n for k in range(3)]\n            value, kind = (sum(sum((p[k] - c[k]) ** 2 for k in range(3)) for p in pts) / n).sqrt(), 'equal_weight_rg'\n        else:\n            raise CheckError('Question does not state which size measure is asked')\n    verify_hashes(key)\n    return dict(judge_numeric(packet, key, value), parameters=dict(measure=kind, atoms=len(pts)),\n                method='Decimal (40 digits) explicit sums')\n\n\n# ---------------------------------------------------------------- inference\ndef force_path(packet, key):\n    \"\"\"dE/dlambda at B = -(path direction) . F(B), summed atom by atom in Decimal.\"\"\"\n    q = packet['question']\n    need('F_i = −∂E/∂r_i' in q, 'Force–gradient relation not stated in the question')\n    if 'starts at B and moves toward A' in q and 'r_i(B) + λ[r_i(A) − r_i(B)]' in q:\n        sign, direction = -1, 'B_to_A'\n    elif 'r_i(A) + λ[r_i(B) − r_i(A)]' in q and 'at λ = 1' in q:\n        sign, direction = 1, 'A_to_B'\n    else:\n        raise CheckError('Path direction not stated unambiguously')\n    files = inputs_by_name(packet)\n    need({'A.xyz', 'B.xyz', 'forces_B.txt'} <= set(files), 'Missing A, B or force table')\n    a, b = read_xyz(files['A.xyz']), read_xyz(files['B.xyz'])\n    rows = [line.split() for line in files['forces_B.txt'].splitlines()[1:] if line.strip()]\n    need(len(a) == len(b) == len(rows), 'A, B and forces differ in atom count')\n    need([e for e, _ in a] == [e for e, _ in b] == [r[1] for r in rows], 'Atom order differs')\n    need([int(r[0]) for r in rows] == list(range(1, len(rows) + 1)), 'Force rows not numbered 1..N')\n    with localcontext() as ctx:\n        ctx.prec = 40\n        projection = sum(Decimal(f) * (pb - pa) for (_, ra), (_, rb), r in zip(a, b, rows)\n                         for pa, pb, f in zip(ra, rb, r[2:]))\n        value = -sign * projection\n    # Provenance: coordinates and forces agree with the public asset to printing precision.\n    [path] = verify_hashes(key).values()\n    data = json.loads(path.read_text())\n    for label, atoms, ref in (('A', a, data['reference_xyz']), ('B', b, data['displaced_xyz'])):\n        need(max(abs(float(c) - r) for (_, p), rr in zip(atoms, ref) for c, r in zip(p, rr)) < 1e-7,\n             label + ' coordinates differ from the source asset')\n    need(max(abs(float(f) - r) for row, rr in zip(rows, data['forces_at_displaced']) for f, r in zip(row[2:], rr)) < 1e-6,\n         'Forces differ from the source asset')\n    return dict(judge_numeric(packet, key, value), parameters=dict(direction=direction, atoms=len(a)),\n                method='Decimal atom-by-atom projection')\n\n\ndef extinction(packet, key):\n    \"\"\"Real/imaginary sums with math.fsum; one option must vanish, three clearly not.\"\"\"\n    q = packet['question']\n    weights_text = find(r'\\(((?:[A-Z][a-z]? = \\d+, )*[A-Z][a-z]? = \\d+)\\)', q,\n                        'Scattering weights not stated in the question').group(1)\n    weights = {k: int(v) for k, v in re.findall(r'([A-Z][a-z]?) = (\\d+)', weights_text)}\n    need('Exactly one of the four reflections' in q, 'Uniqueness premise not stated')\n    [text] = [i['text'] for i in packet['inputs'] if i['format'] == 'fractional-cell-table']\n    lines = text.splitlines()\n    a = float(find(r'cubic a = ([\\d.]+) angstrom', lines[0], 'Cell parameter missing').group(1))\n    atoms = [(r[0], [float(x) for x in r[1:]]) for r in (line.split() for line in lines[1:] if line.strip())]\n    need(all(len(p) == 3 and all(0 <= x < 1 for x in p) for _, p in atoms), 'Fractional coordinates outside [0,1)')\n    need({e for e, _ in atoms} == set(weights), 'Weights do not cover exactly the elements present')\n    total = sum(weights[e] for e, _ in atoms)\n    verdicts = []\n    for o in packet['options']:\n        hkl = [int(x) for x in find(r'^\\((-?\\d+) (-?\\d+) (-?\\d+)\\)$', o['value'], 'Option is not (h k l)').groups()]\n        phase = [2 * math.pi * sum(h * x for h, x in zip(hkl, p)) for _, p in atoms]\n        re_part = math.fsum(weights[e] * math.cos(t) for (e, _), t in zip(atoms, phase))\n        im_part = math.fsum(weights[e] * math.sin(t) for (e, _), t in zip(atoms, phase))\n        verdicts.append((o['label'], o['value'], math.hypot(re_part, im_part) / total))\n    zero = [v for v in verdicts if v[2] <= 1e-6]\n    need(len(zero) == 1, '%d options have |F| = 0 (need exactly 1)' % len(zero))\n    weakest = min(v[2] for v in verdicts if v[2] > 1e-6)\n    need(weakest >= 1e-3, 'A distractor is nearly extinct (|F|/F000 = %.2e)' % weakest)\n    need(key['correct_label'] == zero[0][0], 'Key label %s != recomputed %s' % (key['correct_label'], zero[0][0]))\n    # Provenance: the table must reproduce the public source (cell edge from the CIF,\n    # or every Cartesian row of the published XYZ cell).\n    [path] = verify_hashes(key).values()\n    if path.suffix == '.cif':\n        cif_a = find(r'_cell_length_a\\s+([\\d.]+)', path.read_text(encoding='utf-8'), 'CIF lacks cell edge').group(1)\n        need(abs(float(cif_a) - a) < 1e-4, 'Cell edge differs from the CIF')\n    else:\n        source = read_xyz(path.read_text(encoding='utf-8-sig'))\n        need(len(source) == len(atoms) and all(e == s_e and max(abs(x * a - float(c)) for x, c in zip(f, sp)) < 1e-5\n                                               for (e, f), (s_e, sp) in zip(atoms, source)),\n             'Fractional table does not reproduce the published cell')\n    return dict(label=zero[0][0], recomputed=zero[0][1], parameters=dict(weights=weights, a_A=a, atoms=len(atoms)),\n                options={l: round(r, 8) for l, _, r in verdicts}, weakest_nonzero=round(weakest, 6),\n                method='separate cos/sin sums over the student table')\n\n\n# ---------------------------------------------------------------- design\ndef _bonds(atoms):\n    pts = [p for _, p in atoms]\n    return {(i, j) for i in range(len(pts)) for j in range(i + 1, len(pts))\n            if float(dist(pts[i], pts[j])) <= 1.25 * (RADII[atoms[i][0]] + RADII[atoms[j][0]])}\n\n\ndef _handedness(atoms, bonds):\n    out = {}\n    for i in range(len(atoms)):\n        nb = sorted({b if a == i else a for a, b in bonds if i in (a, b)})\n        if len(nb) == 4:\n            u, v, w = (vec(atoms[i][1], atoms[j][1]) for j in nb[:3])\n            out[i] = dot(cross(u, v), w) > 0\n    return out\n\n\ndef _shape_gap(p, q):\n    pts = range(len(p))\n    diffs = [float(dist(p[i][1], p[j][1]) - dist(q[i][1], q[j][1])) for i in pts for j in pts if i < j]\n    return math.sqrt(math.fsum(d * d for d in diffs) / len(diffs))\n\n\nPOLICY = {'min_energy': ('e_pbe0_mbd_eV', -1, 0.0434), 'max_dipole': ('dipole_eA', 1, 0.10)}\n\n\ndef conformer_selection(packet, key):\n    \"\"\"Constraints from the candidate files themselves; property looked up by matching\n    coordinates in the hash-verified public dataset extract (never by label or id).\"\"\"\n    q = packet['question']\n    if 'lowest total PBE0+MBD energy' in q:\n        goal = 'min_energy'\n    elif 'largest total dipole-moment magnitude' in q:\n        goal = 'max_dipole'\n    else:\n        raise CheckError('Design goal not stated')\n    need('only by rotating about single bonds' in q, 'Allowed modification not stated')\n    files = inputs_by_name(packet)\n    need(set(files) == {'S0.xyz'} | {'candidate_%s.xyz' % l for l in LABELS}, 'Need S0 and four candidates')\n    need(all('Geom-' not in t for t in files.values()), 'Dataset identifiers visible to the model')\n    s0 = read_xyz(files['S0.xyz'])\n    bonds0, hand0 = _bonds(s0), _handedness(s0, _bonds(s0))\n    [path] = verify_hashes(key).values()\n    data = json.loads(path.read_text())\n    prop, sense, margin = POLICY[goal]\n    rows, candidates = [], []\n    for label in LABELS:\n        atoms = read_xyz(files['candidate_%s.xyz' % label])\n        need([e for e, _ in atoms] == [e for e, _ in s0], label + ': atoms or order differ from S0')\n        need(_bonds(atoms) == bonds0, label + ': bonding differs from S0')\n        need(_handedness(atoms, bonds0) == hand0, label + ': configuration at a 4-coordinate centre differs')\n        need(_shape_gap(atoms, s0) > 0.05, label + ': identical to S0 (or its mirror image)')\n        match = [c for c in data['conformers']\n                 if max(abs(float(x) - y) for (_, p), r in zip(atoms, c['xyz']) for x, y in zip(p, r)) < 1e-6]\n        need(len(match) == 1, label + ': no unique dataset structure with these coordinates')\n        candidates.append((label, atoms))\n        rows.append((label, match[0][prop]))\n    for (l1, a1), (l2, a2) in ((x, y) for i, x in enumerate(candidates) for y in candidates[i + 1:]):\n        need(_shape_gap(a1, a2) > 0.05, '%s and %s are the same or mirror conformers' % (l1, l2))\n    ranked = sorted(rows, key=lambda r: sense * r[1], reverse=True)\n    lead = sense * (ranked[0][1] - ranked[1][1])\n    need(lead >= margin, 'Best candidate leads by %.4f, below the %.4f policy margin' % (lead, margin))\n    need(key['correct_label'] == ranked[0][0], 'Key label %s != recomputed %s' % (key['correct_label'], ranked[0][0]))\n    return dict(label=ranked[0][0], recomputed=ranked[0][0], parameters=dict(goal=goal, atoms=len(s0)),\n                options={l: v for l, v in rows}, lead=round(lead, 6), policy_margin=margin,\n                method='own bond graph, handedness and distance-matrix checks; property matched by coordinates')\n\n\nCHECKERS = {\n    'named_bond_angle': named_bond_angle,\n    'backbone_torsion': backbone_torsion,\n    'extent_choice_v2': extent,\n    'force_path_derivative': force_path,\n    'kinematic_extinction': extinction,\n    'conformer_target_selection': conformer_selection,\n}\n",
  "verify_all.py": "\"\"\"Run the independent checkers over every instance of a batch. No model calls.\n\nInput: a family run directory (student-packets.json + private-answers.json) or the\npublished workbench JSON. Exit status 1 if any instance fails, so a batch with a\nsingle disagreement cannot be exported.\n\npython verify_all.py --run runs/family-pilot-v01 --out runs/family-pilot-v01-independent-check.json\npython verify_all.py --workbench docs/data/family-workbench.json --out docs/data/independent-check.json\n\"\"\"\nimport argparse\nimport hashlib\nimport json\nimport sys\nfrom collections import Counter\nfrom pathlib import Path\n\nimport checkers\n\nROOT = Path(__file__).resolve().parent\n\n\ndef load(run=None, workbench=None):\n    if run:\n        run = Path(run)\n        read = lambda n: json.loads((run / n).read_text(encoding='utf-8'))\n        return read('student-packets.json'), read('private-answers.json'), dict(source=str(run.name))\n    data = json.loads(Path(workbench).read_text(encoding='utf-8'))\n    return data['student_packets'], data['teacher_answers'], dict(source=Path(workbench).name, run=data['run'])\n\n\ndef verify(packets, keys, origin):\n    keys = {k['id']: k for k in keys}\n    missing = sorted(set(keys) ^ {p['id'] for p in packets})\n    results = [checkers.check(p, keys[p['id']]) if p['id'] in keys else\n               dict(id=p['id'], family=p['family'], status='fail', problem='No answer key') for p in packets]\n    results += [dict(id=i, family=keys[i]['family'], status='fail', problem='Answer key without packet')\n                for i in missing if i in keys]\n    by_family = {}\n    for r in results:\n        f = by_family.setdefault(r['family'], Counter())\n        f[r['status']] += 1\n    code = sorted((ROOT / 'checkers').glob('*.py')) + [Path(__file__)]\n    return dict(kind='independent_check', checker_version=checkers.VERSION, origin=origin,\n                checked=len(results), passed=sum(r['status'] == 'pass' for r in results),\n                failed=[dict(id=r['id'], problem=r['problem']) for r in results if r['status'] != 'pass'],\n                by_family={k: dict(v) for k, v in by_family.items()},\n                checker_sha256={str(p.relative_to(ROOT)).replace('\\\\', '/'): hashlib.sha256(p.read_bytes()).hexdigest()\n                                for p in code},\n                model_calls=0,\n                scope='Checks input→output, all four options, key agreement, answer-key leakage and source hashes. '\n                      'It does not judge scientific meaning or source interpretation; those remain sampled human/model review.',\n                results=results)\n\n\nif __name__ == '__main__':\n    p = argparse.ArgumentParser(description=__doc__)\n    group = p.add_mutually_exclusive_group(required=True)\n    group.add_argument('--run', type=Path)\n    group.add_argument('--workbench', type=Path)\n    p.add_argument('--out', type=Path, required=True)\n    a = p.parse_args()\n    if a.out.resolve().is_relative_to((ROOT / 'runs').resolve()) and a.out.exists():\n        raise SystemExit('Refusing to overwrite a file under runs/')\n    report = verify(*load(a.run, a.workbench))\n    a.out.write_text(json.dumps(report, ensure_ascii=False, indent=1) + '\\n', encoding='utf-8', newline='\\n')\n    print(json.dumps(dict(checked=report['checked'], passed=report['passed'], failed=report['failed']), ensure_ascii=False))\n    sys.exit(0 if report['passed'] == report['checked'] else 1)\n"
 }
}
