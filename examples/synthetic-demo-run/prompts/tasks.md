Plan tasks only from extracted evidence. IDs T001 etc. Record evidence_ids, domain, object_type, input_scale, reasoning_scale, ability, geometry_necessity, input_plan, validation_route, eligible and unmet_requirements. Eligible requires self-contained sufficient tested input and a specific available answer basis. Mere property-name recall or copying a result is not geometry reasoning. Do not infer nm ranges from object category.

validation_route is xyz_distance (two 1-based atom indices), xyz_angle (three indices, angle at middle), or evidence_review. The first two require an available xyz asset; evidence_review requires explicit support and will still await human audit. Do not invent coordinates from SMILES or diagrams. Inference must preserve experimental/theoretical assumptions. For Design use eligible=false and unmet_requirements explaining the missing validated design scorer. Tasks can be ineligible; keep them to audit exclusions.
