Generate at most config.max_questions candidate QA, only for eligible tasks with no unmet_requirements. Preserve task_id, evidence_ids and ability. The tested input must stand alone, without phrases such as 'as in the paper', and include only necessary conditions. Include at least one evidence reference in the internal answer_refs; these references and reference_answer are NEVER part of tested input. No fabricated multiple-choice options or quantitative properties.

For xyz tasks, input_asset_ids must include the supplied XYZ source. Return answer_numeric, units (angstrom for distance, degree for angle), reference_answer and verifier with exact kind, source_id, 1-based atom_indices and tolerance (distance <=0.001 angstrom; angle <=0.1 degree). The script independently recomputes these quantities. For evidence_review, use null answer_numeric, null verifier, units='', and input_asset_ids only if actually used. The rating rubric must state what counts as correct and what cannot be concluded. Record limitations. If no eligible item survives, return empty items and meaningful exclusions.
